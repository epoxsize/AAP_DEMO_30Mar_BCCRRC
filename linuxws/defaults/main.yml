---
# defaults file for linuxws

#Include conditions
prep_basic: yes
ad_add: yes
setup_firewall: yes
setup_packages: yes

# Defaults for domain tasks
basic_dns_address: '172.17.197.6'
domain_name: 'edu.inno.lab'
domain_ad_user: 'Administrator'
domain_ad_password: 'P@ssw0rd'

# Defaults for ws config
General_config:
  packages:
    nfs-utils: yes
    cifs-utils: yes
  services:
    sshd: yes
  firewall-services:
    sshd: yes
  if_name_template: enp

RedHat_config:
  packages:
    vim-common: yes
  services:
    cockpit: yes

Suse_config:
  if_name_template: eth